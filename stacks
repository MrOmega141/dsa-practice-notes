#include <iostream>
#include <stack>
using namespace std;
int main() {
stack<string> s;
s.push("himang");//to push elements
s.push("second");
s.push("last");
cout<<"the top element is--"<<s.top()<<endl;
s.pop();
cout<<"the top element after popping  is--"<<s.top()<<endl;


	return 0;
}
